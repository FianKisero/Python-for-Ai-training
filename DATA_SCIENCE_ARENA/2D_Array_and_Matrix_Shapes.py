import numpy as np
# Create a 2D array (matrix) with shape (3, 2)
model_runs = np.array([
    [10, 0.85],
    [20, 0.91],
    [30, 0.95]
])
print("Model Runs Matrix:")
print(model_runs)
print("Shape:", model_runs.shape)
